# Scribe Desk

Personal writing app: goals, daily prompts, write/paste/dictate, quick feedback that tunes the next prompt. Data stays in your browser (`localStorage` / `sessionStorage`).

## Run locally

```bash
cd writing-app
npm install
npm run dev
```

## Build

```bash
npm run build
```

Preview the production build with `npm run preview`.
